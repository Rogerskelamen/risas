#include <stdio.h>
#include "common.h"

// read a line from a file
// if current line is has no content,
// return 0
int
fscanl(FILE *fp, char *line)
{
  // while (c = getc(fp)) {
  // }
  return 0;
}
